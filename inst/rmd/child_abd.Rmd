`r i = {{i}}`

# {{i+1}}. Analysis for trait `r traits[i]`

```{r, echo = FALSE}
lc <- st4gi::check.abd(traits[i], treat, rep, data)

if (lc$nt.check.0 > 0) {
  temp <- subset(data, !(data[, treat] %in% lc$check.0))
} else {
  temp <- data
}
temp  
#model <- agricolae::DAU.test(temp[, rep], temp[, treat], temp[, traits[i]])
#model
```
